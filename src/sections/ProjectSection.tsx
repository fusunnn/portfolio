import React, { ReactElement } from "react";

interface Props {}

export default function ProjectSection({}: Props): ReactElement {
  return <div className="h-screen w-screen flex items-center">Projects</div>;
}
